<?xml version="1.0" encoding="utf-8"?>
<!-- rounded_edittext_bg.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="30dp" /> <!-- Adjust the radius to change the roundness -->
    <solid android:color="@android:color/white" /> <!-- Set the background color -->
    <stroke
        android:width="3dp"
        android:color="@color/design_default_color_primary" /> <!-- Add stroke color and width if needed -->
</shape>
